روتر دستگاهی است که شبکه‌ها را به هم وصل می‌کند و داده‌ها را به مقصد درست هدایت می‌کند.  
معمولاً بین شبکه داخلی و اینترنت قرار می‌گیرد و کارهایی مثل NAT و فایروال ساده را انجام می‌دهد.  
روتر باعث می‌شود چند دستگاه بتوانند هم‌زمان از یک اینترنت مشترک استفاده کنند.
